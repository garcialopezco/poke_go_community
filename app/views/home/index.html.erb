<div class="welcome position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center bg-light">
  <div class="col-md-5 p-lg-5 mx-auto my-5">
    <h1 class="display-4 font-weight-normal"><%= t('home.welcome') %></h1>

    <% if !current_user %>
      <%= link_to t('home.login'), login_path, class: "btn btn-outline-secondary"%> OR 
      <%= link_to t('home.sign_up'), sign_up_path, class: "btn btn-outline-secondary" %>
    <% end %>
  </div>
</div>
